# Extras specifically for Jetson AGX Orin deployments.
# Install AFTER requirements.txt, and only after running install_llama_cpp.sh
# (which compiles llama-cpp-python with CUDA support on-device).

# Local LLM inference (installed via install_llama_cpp.sh, listed here for completeness)
# llama-cpp-python>=0.3.0  ; built from source with CMAKE_ARGS="-DGGML_CUDA=on"

# GPU-accelerated numerics (optional — used by deploy/jetson/python/gpu_pricer.py)
# cupy-cuda12x — install manually; no generic ARM64 wheel.
#   Use the Jetson container image or follow:
#   https://docs.cupy.dev/en/stable/install.html#installing-cupy-from-source
# cupy-cuda12x>=12.0 ; sys_platform=='linux'

# jetson-stats wraps tegrastats for easy telemetry.
jetson-stats>=4.0 ; sys_platform=='linux'

# Slightly faster JSON for high-frequency log lines.
orjson>=3.10
